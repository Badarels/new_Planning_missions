import { SearchAdressePipe } from './search-adresse.pipe';

describe('SearchAdressePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAdressePipe();
    expect(pipe).toBeTruthy();
  });
});
